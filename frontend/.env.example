# PACHELARR_BACKEND_URL must match the backend's PACHELARR_PORT (6800 today, NOT the Dockerfile EXPOSE 8080).
PACHELARR_BACKEND_URL=http://localhost:6800
PACHELARR_API_KEY=

# Compose-only: host port for the frontend container (default 3000).
FRONTEND_PORT=3000
